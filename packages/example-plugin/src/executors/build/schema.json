{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Build executor",
  "description": "Example executor. Does nothing.",
  "examples": [
    {
      "command": "nx run one-project:build",
      "description": "If used to run target 'build' on project 'one-project'."
    }
  ],
  "type": "object",
  "properties": {},
  "required": []
}
