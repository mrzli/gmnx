{
  "version": 2,
  "outputCapture": "direct-nodejs",
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "PostgresStop executor",
  "description": "- Stops Postgres docker container.",
  "examples": [
    {
      "command": "nx postgres-stop workspace",
      "description": "Short version."
    },
    {
      "command": "nx run workspace:postgres-stop",
      "description": "Long version."
    }
  ],
  "type": "object",
  "properties": {},
  "required": []
}
